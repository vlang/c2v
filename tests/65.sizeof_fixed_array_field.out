@[translated]
module main

struct Texture {
	name [8]i8
}

fn copy_bytes(dest voidptr, src voidptr, n u32)

fn copy_texture_name(dst &Texture, src &Texture) {
	copy_bytes(dst.name, src.name, sizeof([8]i8))
}
